# Changelog

## 0.2.7

- moved GitHub host/account/session controls off the home dashboard into a dedicated `Settings` page
- added lower status bar actions for `Refresh`, `Login` or `Re-Login`, `Logout`, and direct `Settings` access
- cleaned up the `Control Center` layout so it focuses on repo targeting, cleanup scope, execution, and logs
- updated bundled help files, project metadata, README, and GitHub wiki to match the current app flow
- kept the official square icon pipeline intact and preserved the native app/menu system

## 0.2.6

- moved GitHub auth controls from the first dashboard widget into the lower bar and a dedicated settings workflow
- removed the auth card from the main dashboard and rebalanced the Control Center layout

## 0.2.5

- hardened the square icon pipeline and validated official icon asset usage end to end
